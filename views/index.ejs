<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/home.css">

    <title>Live Site</title>
</head>
<body>
    
    <div class="flexer">
      <div class="watch_out">
          <img src="images/nutriband.png" alt="" class="watcher">
      </div>
     
      
       

      <div class="insider">
        <H1>Your Live update from esp32 cam</H1>
        
        <h2>EanNum<h2 id="dat"><%= EanNum%></h2></h2>
        <h2>message<h2 id="dat"><%= message%></h2></h2>
     
        






        <button class="nutribtn">fake close</button>


      </div>

    </div>
    <script>
        
         function fetchData() {
            fetch('/nutro')
            .then(response => response.json())
            .then(data => {
                //document.getElementById("Ean").innerText = data.EanNum;
                document.getElementById("dat").innerText = data.received;
                //document.getElementsByClassName("try").src = data.mypath;
                //document.getElementById("title").innerText = data.finalz.title;

                // document.getElementById("ingredient").innerText = data.finalz.ingredients;


                // document.getElementById("nutritionfacts").innerText = data.finalz.nutrition_facts;

            })
            .catch(error => console.error('Error fetching data:', error));
        }

  
        setInterval(fetchData, 1000);
/*
        const fetchEan=async()=>{
            try{
                k=await fetch('/nutri')
                Outs=k.json()
            }catch(error){
                console.log("error")
            }
            return Outs

        }
        fetchEan()
            .then(data => {
                document.getElementById("Ean").innerText = data.EanNum;
                document.getElementById("distance").innerText = data.distance;
            })
            .catch(error => console.error('Error fetching data:', error));

        
        setInterval(fetchEan, 1000);*/
    </script>
    

</body>
</html>